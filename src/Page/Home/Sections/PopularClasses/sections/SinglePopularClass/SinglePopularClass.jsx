import PropTypes from "prop-types";
// eslint-disable-next-line no-unused-vars
import React from "react";

const SinglePopularClass = ({singlePopularClass}) => {
    const {image, name, } = singlePopularClass;
	return <div>
        
    </div>;
};

SinglePopularClass.propTypes = {
	singlePopularClass: PropTypes.object
};

export default SinglePopularClass;
